
<h1>Spending by day</h1>

<table>
  <tr>
    <th>Date</th>
    <th>Value of spending</th>
    <th>Share of spending</th>
    <th>Number of purchases</th>
  </tr>
<%for something in @total_by_day  %>
  <tr>
    <td><%=something['transaction_date']%></td>
    <td>$<%=something['sum'].to_i.to_s.chars.to_a.reverse.each_slice(3).map(&:join).join(",").reverse%></td>
    <td><%=((something['sum'].to_f/Transaction.total_all_transactions) * 100).round(1) %>%</td>
    <td><%=something['count']%></td>
  </tr>
<%end%>

</table>